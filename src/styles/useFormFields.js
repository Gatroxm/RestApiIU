import {
  makeStyles
} from '@mui/styles'

const useFormStyles = makeStyles(() => ({
  formField: {
    marginBottom: '2.5em'
  },
  formButtons: {
    marginRight: '1em'
  }
}))

export default useFormStyles
